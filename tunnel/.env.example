# Copy to tunnel/.env on the deploy host (gitignored). Cloudflare dashboard
# -> Zero Trust -> Networks -> Tunnels -> Create a tunnel -> Docker -> copy
# the token shown there.
CLOUDFLARE_TUNNEL_TOKEN=
